[project]
name = "lang_helper_by_delica"
dynamic = ["version"]
authors = [
  { name="Delica Leboe-McGowan", email="stormindustries22@outlook.com" },
]

description = "A Python package that models romance languages (French and Spanish) with object-oriented programming."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICENCE*"]

[build-system]
requires = ["setuptools >= 77.0.3", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"
